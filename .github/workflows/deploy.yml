<<<<<<< Updated upstream
name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true
=======
name: Deploy to GitHub Pages (master branch)

on:
  # 只在 push 到 main 时触发，避免手动误触发
  push:
    branches: [main]
  # 允许在 GitHub UI 上手动触发一次（调试用）
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: "pages-deploy"
  cancel-in-progress: false
>>>>>>> Stashed changes

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< Updated upstream
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
=======
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Verify dist output
        run: |
          test -d dist || (echo "::error::dist/ not generated" && exit 1)
          test -f dist/index.html || (echo "::error::dist/index.html missing" && exit 1)
          echo "dist/ looks good:"
          ls -la dist
>>>>>>> Stashed changes

  deploy:
    needs: build
    runs-on: ubuntu-latest
<<<<<<< Updated upstream
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
=======
    steps:
      - name: Checkout main (build output)
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Push dist/ to master branch
        run: |
          git config --local user.email "github-actions[bot]"
          git config --local user.name "github-actions"

          # 切到一个干净的临时分支，避免把 main 的源码推到 master
          git checkout --orphan temp-pages
          git rm -rf .
          git clean -fdx

          # 把构建产物拷到工作树根目录
          cp -r dist/. .

          # 保留仓库的 .gitignore 等元文件（可选，按需删除）
          # 这里不保留，确保 master 分支就是纯 dist 内容

          git add .
          git commit -m "deploy: $(date +%Y-%m-%d-%H:%M:%S) from ${GITHUB_SHA}"
          git branch -M master
          git push -f origin master
>>>>>>> Stashed changes
