{
  "name": "njs-workflow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "guard-audit": "node scripts/guard-audit.mjs",
    "lint": "npm run guard-audit",
    "check": "npm run lint && npm run build"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "vite": "^7.1.3"
  }
}
